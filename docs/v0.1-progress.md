# v0.1 进度记录

> 截至 2026-06-15

## 已完成（代码部分）

| 子版本 | 状态 | Commit | 说明 |
|--------|------|--------|------|
| v0.1.0 schema 文档 | ✅ | `68f92e1` | 重写 database-schema.md（补 views/history、确定性 _id、collectCount、权限、索引） |
| v0.1.0 集合 + 权限 | ✅ 手动 | — | 用户在云开发控制台建了 7 集合，配了权限 |
| v0.1.0 云函数部署 | ✅ 手动 | — | login / updateProfile / interact 已部署 |
| v0.1.1 updateProfile | ✅ | `35d6af3` | 改用 `_id=OPENID` upsert + 补 brief/level + avatarUrl 字段名统一 |
| v0.1.2 createPost 清理 | ✅ | `e407997` `1b6ad05` | 删遗留 `comments` 字段 + 加 `collectCount`；修复 `_openid` 手动注入导致 `Invalid Key Name` 错误 |
| v0.1.3 interact 云函数 | ✅ | `54f90de` | 6 action（view/like/unlike/collect/uncollect/comment），确定性 _id 幂等，统一返回结构，COUNT_UPDATE_FAILED 失败边界 |
| v0.1.4 db.js 改造 | ✅ | `f29f76b` | callInteract 封装 + toggleLike/toggleCollect/addComment 云就绪走 interact |
| v0.1.4 detail.js 改造 | ✅ | `4631424` | 浏览/点赞/收藏/评论全走 interact，按返回值刷 UI |
| v0.1.0~v0.1.2 联调验证 | ✅ | — | 云环境连通确认，发帖成功写入 posts 集合 |

## 测试验证情况

- **云环境连通**：app.js initCloud 成功（53-95ms），cloudReady=true。
- **发帖走通真实云**：修掉 `_openid` 手动注入问题后，发帖成功写入云端 `posts` 集合，字段含 `collectCount: 0`、`commentCount: 0`，无遗留 `comments` 字段。
- **interact 云函数**：已部署，待 A/B 双人测试验证（Task 8）。

## 未完成

| 子版本 | 状态 | 阻塞 |
|--------|------|------|
| v0.1.5 多人体验版验收 | ⏳ 待做 | 需两个体验成员账号端到端测试 14 个场景 |
| v0.1.5 体验版发布 | ⏳ 待做 | 需上传版本 + 设体验版 + 加成员 |
| v0.1.6 文档与遗留清理 | ⏳ 待做 | 需写 cloudfunctions.md + 标注 mock fallback |

## 已知问题

### 1. 发布页 placeholder 错位（WIP）

`pages/publishForm/index.wxml` 的 `input` 和 `textarea` 是微信原生组件，placeholder 在模拟器中错位/显示不全。已尝试加 `placeholder-class` 和统一 `font-size`/`line-height`，未完全解决。下次处理方向：检查 `scroll-view` 内原生组件渲染兼容性，或改用非原生输入方案。

相关 commit：`ac05315`（WIP）

### 2. add() catch 打印了错误日志但未彻底清理

`utils/db.js:262` 的 `add` 函数 catch 块目前会 `console.error` 打印降级日志（v0.1 调试用），v0.2 需要按 mock fallback 策略重构统一处理。

### 3. project.config.json cloudfunctionRoot 路径

调试时从 `cloudfunctions/` 改成了 `/cloudfunctions`，需确认对云函数部署无影响。

## commit 历史（v0.1 本轮）

```
ac05315 fix(publishForm): 修 placeholder 错位（WIP，仍有原生组件渲染问题）
1b6ad05 fix(db): createPost 去除手动 _openid，add catch 打印错误日志
4631424 feat(detail): 浏览/点赞/收藏/评论走 interact，按返回值刷 UI
f29f76b feat(db): callInteract 封装 + toggleLike/toggleCollect/addComment 云就绪走 interact
54f90de feat(interact): 新增 6 action 互动云函数
e407997 refactor(db): createPost 去除遗留 comments 字段，新增 collectCount
35d6af3 feat(updateProfile): 改用 _id=OPENID upsert
68f92e1 docs: 重写 database-schema.md
0c9a780 docs: 重构迭代规划为细分小版本
```

## 下次继续的入口

1. **修发布页 placeholder 问题**（用户优先关注）
2. **v0.1.5 多人测试**（需要第二个微信账号配合）
3. **v0.1.6 补 cloudfunctions.md 文档**
4. **push 到远程**（当前 9 个 commit 未 push）
