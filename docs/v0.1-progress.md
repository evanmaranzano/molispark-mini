# v0.1 进度记录

> 截至 2026-06-16

## 已完成（代码 + 文档）

| 子版本 | 状态 | Commit | 说明 |
|--------|------|--------|------|
| v0.1.0 schema 文档 | ✅ | `68f92e1` | 重写 database-schema.md（补 views/history、确定性 _id、collectCount、权限、索引） |
| v0.1.0 集合 + 权限 | ✅ 手动 | — | 云控制台建 7 集合并配权限 |
| v0.1.0 云函数部署 | ✅ 手动 | — | login / updateProfile / interact 已部署 |
| v0.1.1 updateProfile | ✅ | `35d6af3` | `_id=OPENID` upsert + brief/level + avatarUrl 统一 |
| v0.1.2 createPost 清理 | ✅ | `e407997` `1b6ad05` | 删遗留 `comments` 字段 + 加 collectCount；修 `_openid` 手动注入 `Invalid Key Name` |
| v0.1.3 interact 云函数 | ✅ | `54f90de` | 6 action，确定性 _id 幂等，统一返回，COUNT_UPDATE_FAILED 边界 |
| v0.1.4 db.js 改造 | ✅ | `f29f76b` | callInteract + toggleLike/toggleCollect/addComment 云就绪走 interact |
| v0.1.4 detail.js 改造 | ✅ | `4631424` | 浏览/点赞/收藏/评论走 interact，按返回值刷 UI |
| v0.1.6 cloudfunctions.md | ✅ | 本次 | 3 云函数接口文档（入参/返回/错误码/幂等/部署） |
| v0.1.6 mock fallback 标注 | ✅ | 本次 | db.js 顶部注释标注多模式降级策略 |
| 发布页 placeholder 修复 | ✅ 待视觉验证 | 本次 | input 显式高度+line-height 居中、textarea 固定 rpx 行高、placeholder 继承组件行高 |
| cloudfunctionRoot 修复 | ✅ | 本次 | `/cloudfunctions` → `cloudfunctions/`（恢复尾部斜杠，CLAUDE.md 硬规则） |
| login 头像字段名 | ✅ | 本次 | 读 `avatarFileID` → `avatarUrl`，与 updateProfile 统一（需重新部署 login） |
| 评论端到端修复 | ✅ | 本次 | addComment/saveDraft/submitFeedback 去手动 `_openid`（Invalid Key Name）；addComment 传 `body`/`name`；interact comment 写 `body`/`name` 对齐前端；detail 去评论 mock fallback（不再显示假评论）；需重新部署 interact |

## 测试验证情况

- **云环境连通**：app.js initCloud 成功（53-95ms），cloudReady=true。
- **发帖走通真实云**：写入云端 `posts`，含 `collectCount:0` `commentCount:0`，无遗留 `comments`。
- **interact 云函数**：已部署，待双人端到端验证（Task 8）。
- **页面配置校验**：`npm run validate:pages` 通过（15 路由）。
- **发布页 placeholder**：代码已改，需在开发者工具/真机视觉确认错位消除。

## 未完成（均需用户手动 + 第二账号）

| 子版本 | 状态 | 阻塞 |
|--------|------|------|
| v0.1.5 多人体验版验收 | ⏳ 待做 | 需 A/B(/C) 账号端到端跑 plan Task 8 的 10 步 |
| v0.1.5 体验版发布 | ⏳ 待做 | 上传 0.1.0 + 设体验版 + 加成员 + 分享二维码（plan Task 9） |

> Task 8/9 完整步骤见 `docs/superpowers/plans/2026-06-15-v0.1-cloud-backend-mvp.md`，照做即可，无代码改动。

## 已知问题

### 1. 发布页 placeholder 错位 — 已修，待视觉验证

`pages/drafts/index.less`（publishForm / feedback 共享）：单行 input 改 `height:80rpx + line-height:80rpx + padding:0 20rpx` 垂直居中；textarea 固定 `line-height:44rpx`；`.form-placeholder` 去掉 line-height 改为继承组件固定行高（消除无单位倍数在原生层的歧义）。需开发者工具确认；若仍有残留，看截图后可能微调数值或查 scroll-view 内原生组件兼容。

### 2. 头像字段名不一致 — 已修

`cloudfunctions/login/index.js` 原读 `user.avatarFileID`，updateProfile 写 `avatarUrl`，导致登录 profile.avatarUrl 取不到值。已改为读 `user.avatarUrl`。**需重新部署 login 云函数才生效。**

### 3. db.js add() catch 降级日志

`utils/db.js` 的 `add` catch 块 `console.error` 打印降级日志（v0.1 调试用），v0.2 抽 mock fallback 中间层时统一移除。已在顶部注释标注。

### 4. cloudfunctionRoot — 已修

`project.config.json` 从 `/cloudfunctions` 改回 `cloudfunctions/`（CLAUDE.md 第 56 行要求尾部斜杠），否则 UI 增量上传可能报 `Cannot read property 'region' of undefined`。

## 下次继续的入口

代码 + 文档已全部完成并 push 到 feature/update。只剩用户手动部分：
1. **修头像字段名不一致**（login 读 avatarUrl）——小改，建议验收前做
2. **v0.1.5 双人/三人验收**（plan Task 8，需第二账号）——v0.1 收尾的唯一阻塞
3. **v0.1.5 体验版发布**（plan Task 9）
4. 发布页 placeholder 在开发者工具视觉确认

---

# 2026-07-28 追加：活动报名 + 手机号快捷登录

## 已完成（代码）

| 项目 | 说明 |
|------|------|
| 活动报名全栈 | 新增 `activities` / `signups` 集合（schema 见 docs/database-schema.md）；`activity` 云函数（signup/cancel，确定性 _id `${activityId}_${openid}` 幂等，名额校验，取消不删文档可恢复）；db.js `callActivity/signupActivity/cancelSignup/getActivities/getActivityById/getMySignups`（含本地降级）；`seed` 云函数预置 3 个活动 |
| 页面 | `pages/activities`（列表，首页 quickActions + 我的菜单入口）、`pages/activityDetail`（详情+报名表单+取消，未登录弹 login-modal）、`pages/myActivities`（我的报名，loginGuard 守卫） |
| 手机号快捷登录 | login-modal 新增「手机号快捷登录」按钮（`open-type="getPhoneNumber"`）；login 云函数 `action='phone'` 用 code 换手机号并写 `users.phoneNumber`；auth.js 新增 `updateSessionProfile` |
| 文档 | database-schema.md 补 2 集合 + users.phoneNumber；cloudfunctions.md 补 login phone 分支 + activity + seed；CLAUDE.md 集合数 7→9 |

## 待手动（云端/后台）

1. 云控制台新建 2 个集合并配权限：`activities`（所有用户可读，仅创建者可写）、`signups`（仅创建者可读写）
2. 部署 3 个云函数：`activity`（新建）、`login`（phone 分支）、`seed`（活动预置，部署后调用一次灌数据）
3. 手机号快捷登录：mp 后台需非个人主体 + 开通「手机号快速验证」（按次计费）；未开通时按钮会收到 `PHONE_FAILED` 并提示走微信登录，属预期降级
4. 真机/开发者工具回归：活动列表 → 报名 → 我的报名 → 取消 → 名额恢复；登录弹窗两个按钮

## 验证

- `npm run validate:pages` 通过（18 路由，新增 3 页）
- `node --check` 全部改动文件语法通过

---

# 2026-07-28 追加二：删除论坛 + 专区改精选 + 管理员权限体系

## 已完成（代码）

| 项目 | 说明 |
|------|------|
| 删除论坛 | 删 pages/forum（目录、路由）；首页「更多 ›」改指精选页；quickActions 移除论坛入口；消息页/mock 残留文案清理 |
| 专区改精选 | zones 页改为加精帖列表（`posts.featured = true`，本地 mock 降级用 featured 标记的示例帖）；首页/搜索占位文案同步改「精选」 |
| 管理员权限 | `users.role`（admin/member，控制台手动授权）；interact 新增 feature/unfeature（云函数端校验 admin）；activity 新增 create（同校验）；login profile 返回 role |
| 客户端 | detail 页管理员可见加精/取消加精按钮；activities 页管理员可见「发布新活动」入口；新增 pages/activityPublish 表单页（loginGuard + admin 双门禁） |

## 待手动（云端）

1. 重新部署云函数：`interact`、`activity`、`login`
2. 授权管理员：云控制台 → users 集合 → 找到目标用户文档 → 加字段 `role: 'admin'`
3. 回归：管理员账号详情页加精 → 精选页可见；发布活动 → 列表可见；非管理员无入口且云函数返回 FORBIDDEN

## 验证

- `npm run validate:pages` 通过
- `node --check` 全部改动文件语法通过

---

# 2026-07-28 追加三：删除活动 + 发帖时间修复 + isAdmin 兼容

- activity 云函数新增 `remove`（admin）：先清 signups 再删活动；db.js `removeActivity`；activityDetail 页管理员可见红色「删除活动」按钮（二次确认）。
- 发帖时间：新增 `utils/time.js`（formatTime：Date/ISO/时间戳 → 刚刚/N分钟前/N小时前/N天前/YYYY-MM-DD，非时间字符串原样透传），home/zones/favorites/history/myPosts/drafts/search/detail（含评论时间）统一接入；修复 createPost 静态写入「刚刚」及云端 Date 对象直接渲染的问题。
- isAdmin 兼容：interact/activity 的管理员判断改为 where({openid}) + doc(openid) 双查，修复 users 集合存在 auto _id 旧档时「按钮显示但 FORBIDDEN」的问题。

---

# 2026-07-28 v0.2 全版本收尾（v0.2.0–v0.2.5 全部完成）

> v0.2 原计划见 docs/superpowers/specs/2026-06-15-login-backend-iteration-design.md；技术路线有调整，功能目标全部达成。

| 小版本 | 原计划 | 实际落地 |
|--------|--------|----------|
| v0.2.0 mock/fallback 重构 | 生产禁止静默降级 | ✅ 7-13 已提前完成 |
| v0.2.1 草稿真实云 | 新建 drafts 集合 | ✅ 走 `posts.status='draft'`（未单建集合），保存/编辑/删除/发布全通云 |
| v0.2.2 收藏/历史真实云 | 读 collects/history + 取消收藏/删除历史 | ✅ 收藏页新增「取消收藏」按钮（catchtap 阻断跳详情）；历史页新增「删除记录」（db.js `deleteHistory` → removeWhere） |
| v0.2.3 消息/反馈真实云 | messages/feedback 接真实数据 | ✅ 消息页早已读云（本轮补时间格式化）；反馈提交走云并带状态提示 |
| v0.2.4 全页面状态优化 | loading/empty/error/retry | ✅ 11 个页面错误态补「点击重试」（retryLoad），detail 页本就有重试 |
| v0.2.5 权限收紧与数据清理 | 权限复查/遗留字段/索引 | ✅ db.js 移除 v0.1 调试日志；createPost 去掉静态 `time:'刚刚'` 遗留字段；删除无引用的 zones mock、zone-card/segment/tag 死样式；schema 补齐 11 集合权限表与索引清单 |

## 待手动（控制台）

1. **索引补建**（仅控制台可操作）：database-schema.md「索引建议」表，重点 posts.createdAt、posts.status、comments.postId、signups.openid
2. **集合确认**：views/history 两个集合若 interact 尚未真实写入过，需在控制台手动建（仅创建者可读写）

---

# 2026-07-29 追加：视频上传 + 首页轮播

- 帖子视频：storage.js `chooseAndUploadVideo`（≤50MB）；publishForm 视频选择/预览/移除，posts.videos 入库；detail 页 video 播放（getTempFileURL 转换）
- 活动视频：activity 云函数 create 接受 videos（≤3）；activityPublish 上传入口；activityDetail 播放
- 首页 hero 改 swiper 轮播 3 页：品牌卡（了解精选）/ 摩力沙龙海报（点进活动详情）/ 近期活动（进活动列表）
- 注意：视频走云存储 CDN 流量（套餐 5GB/月），放量后评估点播服务
