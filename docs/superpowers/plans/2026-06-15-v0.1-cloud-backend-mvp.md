# v0.1 实施计划（细分子版本 v0.1.0–v0.1.6）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让体验成员完成 登录→完善资料→浏览→发帖→评论→点赞/收藏 的完整闭环，全部走真实微信云开发。

**Architecture:** 新增 `interact` 云函数统一处理 view/like/unlike/collect/uncollect/comment 的「子集合写入 + posts 计数」（绕过集合「仅创建者可写」权限）。客户端 `db.js` 云就绪时走 `callInteract`，失败降级本地。准入用微信后台体验成员白名单（0 代码）。

**Tech Stack:** 微信小程序 + TDesign miniprogram + 微信云开发（云数据库 / 云函数 wx-server-sdk / 云存储）

**验证策略（重要）：** 本项目**无自动化测试框架**（小程序运行于微信开发者工具，云函数依赖 wx-server-sdk）。各 task 验证用：
- `node --check <file>` 语法检查（CommonJS 文件：云函数、utils/*.js）
- `npm run validate:pages` 页面结构验证
- 微信开发者工具编译预览（页面 wxml/js）
- 云函数部署后真机/预览端到端测试

**对应 spec:** `docs/superpowers/specs/2026-06-15-login-backend-iteration-design.md`

---

## File Structure

| 文件 | 责任 | 操作 |
|------|------|------|
| `docs/database-schema.md` | 集合 schema 文档 | 新建 |
| `cloudfunctions/interact/package.json` | 云函数依赖声明 | 新建 |
| `cloudfunctions/interact/index.js` | 6 action 统一计数入口（确定性 _id 幂等） | 新建 |
| `cloudfunctions/updateProfile/index.js` | 用户资料 upsert（已有骨架，需确认） | 确认/修改 |
| `utils/db.js` | 加 `callInteract`；`toggleLike`/`addComment`/`toggleCollect`/`incrementViews` 云就绪走 interact；清 `createPost` 遗留 `comments` 字段 | 修改 |
| `pages/detail/index.js` | 浏览/点赞/收藏/评论走 callInteract；按返回值刷新 UI | 修改 |
| `pages/history/index.js` | 确认读取 history 集合正常 | 确认 |

**不动的文件：** `pages/publishForm/index.js:114` 的 `updatePost` 是作者改自己帖子 status，权限允许，**不走 interact**。

---

## Task 1: v0.1.0 云环境与数据库基线确认

**Files:**
- Create: `docs/database-schema.md`

### Step 1: 写数据库 schema 文档

创建 `docs/database-schema.md`，记录 7 个集合（含实际在用但文档遗漏的 `history` 和 `views`）：

```
# 集合清单（v0.1）

| 集合 | _id 策略 | 权限 |
|------|----------|------|
| users | OPENID | 仅创建者可读写 |
| posts | 自增 | 所有用户可读，仅创建者可写 |
| comments | 自增 | 所有用户可读，仅创建者可写 |
| likes | `${postId}_${OPENID}` | 仅创建者可读写 |
| collects | `${postId}_${OPENID}` | 仅创建者可读写 |
| views | `${postId}_${OPENID}_${YYYYMMDD}` | 仅创建者可读写 |
| history | `${postId}_${OPENID}` | 仅创建者可读写 |
```

每个集合列出字段、类型、说明。

- [ ] 写 `docs/database-schema.md`

### Step 2: 确认 envId 已配

已核实：`app.js:36` 已配 `cloud1-d4gtpsssef2dcbcf8`，非占位。无需改动。

- [ ] 确认 app.js envId 正确（已核实）

### Step 3: Commit

```bash
cd /f/molispark/mini
git add docs/database-schema.md
git commit -m "docs: 新增数据库 schema 文档（含 history/views 集合）"
```

- [ ] Commit

---

## Task 2: v0.1.1 确认 updateProfile 云函数

**Files:**
- 确认/修改: `cloudfunctions/updateProfile/index.js`

### Step 1: 检查现有 updateProfile 实现

读取 `cloudfunctions/updateProfile/index.js`，确认：
- 从 `cloud.getWXContext()` 取 OPENID（不接受客户端传 openid）
- `users._id = OPENID` 做 upsert
- 字段包含 nickName、avatarUrl、brief、createdAt、updatedAt

- [ ] 确认 updateProfile 实现

### Step 2: 补 level 字段（如缺失）

如 updateProfile 未写 `level` 字段，补上默认值 `level: 0`。

- [ ] 补 level 字段（如需要）

### Step 3: 语法验证

```bash
cd /f/molispark/mini && node --check cloudfunctions/updateProfile/index.js
```

- [ ] 语法验证

### Step 4: Commit（如有改动）

```bash
cd /f/molispark/mini
git add cloudfunctions/updateProfile/index.js
git commit -m "feat(updateProfile): 确认 OPENID upsert + level 字段"
```

- [ ] Commit（如有改动）

---

## Task 3: v0.1.2 清 createPost 遗留 comments 字段

**Files:**
- Modify: `utils/db.js:419-434`（`createPost` 函数）

### Step 1: 删除 comments 字段

在 `createPost` 的 `add('posts', {...})` 数据对象中，删除 `comments: 0,` 行。同时新增 `collectCount: 0,`。

改动后该片段：
```js
    views: 0,
    likes: 0,
    collectCount: 0,
    commentCount: 0,
    _openid: getCurrentOpenid(),
```

- [ ] 删 comments、加 collectCount

### Step 2: 语法验证

```bash
cd /f/molispark/mini && node --check utils/db.js
```

- [ ] 语法验证

### Step 3: Commit

```bash
cd /f/molispark/mini
git add utils/db.js
git commit -m "refactor(db): createPost 去除遗留 comments 字段，新增 collectCount"
```

- [ ] Commit

---

## Task 4: v0.1.3 创建 interact 云函数（6 action）

**Files:**
- Create: `cloudfunctions/interact/package.json`
- Create: `cloudfunctions/interact/index.js`

### Step 1: 写 package.json

`cloudfunctions/interact/package.json`：
```json
{
  "name": "interact",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "wx-server-sdk": "^3.0.1"
  }
}
```

- [ ] 写 package.json

### Step 2: 写 index.js（完整实现 6 action）

`cloudfunctions/interact/index.js`。核心设计：

```js
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });

const db = cloud.database();
const _ = db.command;

// 生成日期字符串 YYYYMMDD
function getDateStr() {
  const d = new Date();
  return `${d.getFullYear()}${String(d.getMonth() + 1).padStart(2, '0')}${String(d.getDate()).padStart(2, '0')}`;
}

// 统一返回结构
function makeResponse(action, postId, { state, counts, data, success = true, code }) {
  const res = { success, action, postId };
  if (code) { res.code = code; return res; }
  res.state = state;
  res.counts = counts;
  if (data) res.data = data;
  return res;
}

// 查帖子并取当前计数
async function getPostCounts(postId) {
  const postRes = await db.collection('posts').doc(postId).get();
  const p = postRes.data;
  return {
    post: p,
    counts: {
      likes: p.likes || 0,
      collectCount: p.collectCount || 0,
      commentCount: p.commentCount || 0,
      views: p.views || 0,
    },
  };
}

exports.main = async (event) => {
  const { OPENID } = cloud.getWXContext();
  if (!OPENID) return { success: false, code: 'UNAUTHORIZED' };

  const { action, postId, content } = event;
  if (!postId) return { success: false, code: 'MISSING_POST_ID' };
  if (!['view', 'like', 'unlike', 'collect', 'uncollect', 'comment'].includes(action)) {
    return { success: false, code: 'INVALID_ACTION' };
  }

  // 校验帖子存在
  let postInfo;
  try {
    postInfo = await getPostCounts(postId);
  } catch (e) {
    return { success: false, code: 'POST_NOT_FOUND' };
  }

  const { post, counts } = postInfo;

  try {
    // ── view ──
    if (action === 'view') {
      const viewId = `${postId}_${OPENID}_${getDateStr()}`;
      let isFirstView = false;
      try {
        await db.collection('views').add({ data: { _id: viewId, postId, openid: OPENID, viewedAt: db.serverDate() } });
        isFirstView = true;
      } catch (e) {
        // 主键冲突 = 今天已浏览过，不重复 inc
      }
      if (isFirstView) {
        try {
          await db.collection('posts').doc(postId).update({ data: { views: _.inc(1) } });
          counts.views += 1;
        } catch (e) {
          console.error('view count inc failed:', postId, OPENID, e.message);
          return makeResponse(action, postId, { success: false, code: 'COUNT_UPDATE_FAILED' });
        }
      }
      // upsert history 快照
      const historyId = `${postId}_${OPENID}`;
      try {
        await db.collection('history').doc(historyId).set({
          data: {
            postId,
            title: post.title || '',
            cover: post.cover || (post.images && post.images[0]) || '',
            category: post.category || '',
            authorName: post.author || '',
            viewedAt: db.serverDate(),
            updatedAt: db.serverDate(),
          },
        });
      } catch (e) {
        // history 失败不影响 view 主流程
        console.warn('history upsert failed:', postId, OPENID, e.message);
      }
      return makeResponse(action, postId, { state: {}, counts });
    }

    // ── like ──
    if (action === 'like') {
      const likeId = `${postId}_${OPENID}`;
      let created = false;
      try {
        await db.collection('likes').add({ data: { _id: likeId, postId, openid: OPENID, createdAt: db.serverDate() } });
        created = true;
      } catch (e) {
        // 主键冲突 = 已点赞，幂等返回
      }
      if (created) {
        try {
          await db.collection('posts').doc(postId).update({ data: { likes: _.inc(1) } });
          counts.likes += 1;
        } catch (e) {
          console.error('like count inc failed:', postId, OPENID, e.message);
          return makeResponse(action, postId, { success: false, code: 'COUNT_UPDATE_FAILED' });
        }
      }
      return makeResponse(action, postId, { state: { liked: true, collected: counts.collected }, counts });
    }

    // ── unlike ──
    if (action === 'unlike') {
      const likeId = `${postId}_${OPENID}`;
      let removed = false;
      try {
        const removeRes = await db.collection('likes').doc(likeId).remove();
        removed = removeRes.stats.removed > 0;
      } catch (e) {
        // 文档不存在，幂等返回
      }
      if (removed) {
        try {
          await db.collection('posts').doc(postId).update({ data: { likes: _.inc(-1) } });
          counts.likes = Math.max(0, counts.likes - 1);
        } catch (e) {
          console.error('unlike count inc failed:', postId, OPENID, e.message);
          return makeResponse(action, postId, { success: false, code: 'COUNT_UPDATE_FAILED' });
        }
      }
      return makeResponse(action, postId, { state: { liked: false, collected: counts.collected }, counts });
    }

    // ── collect ──
    if (action === 'collect') {
      const collectId = `${postId}_${OPENID}`;
      let created = false;
      try {
        await db.collection('collects').add({ data: { _id: collectId, postId, openid: OPENID, createdAt: db.serverDate() } });
        created = true;
      } catch (e) {
        // 主键冲突 = 已收藏，幂等返回
      }
      if (created) {
        try {
          await db.collection('posts').doc(postId).update({ data: { collectCount: _.inc(1) } });
          counts.collectCount += 1;
        } catch (e) {
          console.error('collect count inc failed:', postId, OPENID, e.message);
          return makeResponse(action, postId, { success: false, code: 'COUNT_UPDATE_FAILED' });
        }
      }
      return makeResponse(action, postId, { state: { liked: counts.liked, collected: true }, counts });
    }

    // ── uncollect ──
    if (action === 'uncollect') {
      const collectId = `${postId}_${OPENID}`;
      let removed = false;
      try {
        const removeRes = await db.collection('collects').doc(collectId).remove();
        removed = removeRes.stats.removed > 0;
      } catch (e) {
        // 文档不存在，幂等返回
      }
      if (removed) {
        try {
          await db.collection('posts').doc(postId).update({ data: { collectCount: _.inc(-1) } });
          counts.collectCount = Math.max(0, counts.collectCount - 1);
        } catch (e) {
          console.error('uncollect count inc failed:', postId, OPENID, e.message);
          return makeResponse(action, postId, { success: false, code: 'COUNT_UPDATE_FAILED' });
        }
      }
      return makeResponse(action, postId, { state: { liked: counts.liked, collected: false }, counts });
    }

    // ── comment ──
    if (action === 'comment') {
      if (!content || !content.trim()) return { success: false, code: 'EMPTY_CONTENT' };
      if (content.length > 500) return { success: false, code: 'CONTENT_TOO_LONG' };

      const commentRes = await db.collection('comments').add({
        data: { postId, openid: OPENID, content: content.trim(), createdAt: db.serverDate() },
      });
      try {
        await db.collection('posts').doc(postId).update({ data: { commentCount: _.inc(1) } });
        counts.commentCount += 1;
      } catch (e) {
        console.error('comment count inc failed:', postId, OPENID, e.message);
        return makeResponse(action, postId, { success: false, code: 'COUNT_UPDATE_FAILED' });
      }
      return makeResponse(action, postId, {
        state: {},
        counts,
        data: { commentId: commentRes._id },
      });
    }
  } catch (err) {
    console.error('interact failed:', action, postId, OPENID, err.message);
    return { success: false, code: 'INTERNAL_ERROR', error: err.message };
  }
};
```

- [ ] 写 index.js

### Step 3: 语法验证

```bash
cd /f/molispark/mini && node --check cloudfunctions/interact/index.js
```

- [ ] 语法验证

### Step 4: Commit

```bash
cd /f/molispark/mini
git add cloudfunctions/interact/package.json cloudfunctions/interact/index.js
git commit -m "feat(interact): 新增 6 action 互动云函数（确定性 _id 幂等 + 统一返回结构 + 失败边界）"
```

- [ ] Commit

---

## Task 5: v0.1.4 db.js 加 callInteract + 改造互动方法

**Files:**
- Modify: `utils/db.js`

### Step 1: 加 callInteract 函数

在 `utils/db.js` 的 `col` 函数之前插入：
```js
async function callInteract(payload) {
  if (!isCloudReady()) return null;
  try {
    const res = await wx.cloud.callFunction({ name: 'interact', data: payload });
    return res.result;
  } catch (err) {
    console.error('callInteract failed:', err);
    return null;
  }
}
```

- [ ] 加 callInteract

### Step 2: 加入导出

在 `module.exports` 对象内加 `callInteract,`。

- [ ] 加导出

### Step 3: 改造 toggleLike（云就绪走 interact）

将 `toggleLike`（约 462 行）整体替换为：
```js
async function toggleLike(postId, openid) {
  const existing = await query('likes', { postId, openid }, { limit: 1 });
  const willLike = existing.length === 0;

  if (isCloudReady()) {
    const result = await callInteract({ action: willLike ? 'like' : 'unlike', postId });
    if (result && result.success) {
      return {
        liked: result.state.liked,
        likes: result.counts.likes,
      };
    }
    // interact 失败则降级本地
  }

  if (willLike) {
    await add('likes', { postId, openid, createdAt: serverDate() });
    await updateById('posts', postId, { likes: _.inc(1) });
    return { liked: true };
  }
  await removeById('likes', existing[0]._id);
  await updateById('posts', postId, { likes: _.inc(-1) });
  return { liked: false };
}
```

- [ ] 改 toggleLike

### Step 4: 改造 toggleCollect（云就绪走 interact）

将 `toggleCollect`（约 479 行）整体替换为：
```js
async function toggleCollect(postId, openid) {
  const existing = await query('collects', { postId, openid }, { limit: 1 });
  const willCollect = existing.length === 0;

  if (isCloudReady()) {
    const result = await callInteract({ action: willCollect ? 'collect' : 'uncollect', postId });
    if (result && result.success) {
      return {
        collected: result.state.collected,
        collectCount: result.counts.collectCount,
      };
    }
  }

  if (willCollect) {
    await add('collects', { postId, openid, createdAt: serverDate() });
    return { collected: true };
  }
  await removeById('collects', existing[0]._id);
  return { collected: false };
}
```

- [ ] 改 toggleCollect

### Step 5: 改造 addComment（云就绪走 interact）

将 `addComment`（约 452 行）整体替换为：
```js
async function addComment(data) {
  if (isCloudReady()) {
    const result = await callInteract({ action: 'comment', postId: data.postId, content: data.content });
    if (result && result.success) {
      return { _id: result.data.commentId, commentCount: result.counts.commentCount };
    }
  }

  const result = await add('comments', {
    ...data,
    _openid: getCurrentOpenid(),
    createdAt: serverDate(),
  });
  await updateById('posts', data.postId, { commentCount: _.inc(1) });
  return result;
}
```

- [ ] 改 addComment

### Step 6: 语法验证

```bash
cd /f/molispark/mini && node --check utils/db.js
```

- [ ] 语法验证

### Step 7: Commit

```bash
cd /f/molispark/mini
git add utils/db.js
git commit -m "feat(db): callInteract 封装 + toggleLike/toggleCollect/addComment 云就绪走 interact"
```

- [ ] Commit

---

## Task 6: v0.1.4 detail.js 改走 callInteract

**Files:**
- Modify: `pages/detail/index.js`

### Step 1: import 加 callInteract

在 `pages/detail/index.js` 顶部 import 花括号内加入 `callInteract`。

- [ ] 改 import

### Step 2: 改造 incrementViews（走 interact view）

将 `incrementViews`（约 89 行）替换为：
```js
  async incrementViews(post) {
    try {
      const result = await callInteract({ action: 'view', postId: this.postId });
      if (result && result.success) {
        this.setData({ 'post.views': result.counts.views });
        return;
      }
      // interact 失败降级
      await updatePost(this.postId, { views: _.inc(1) });
      this.setData({ 'post.views': (post.views || 0) + 1 });
    } catch (err) {
      // 静默失败
    }
  },
```

- [ ] 改 incrementViews

### Step 3: 改造点赞/收藏/评论回调

详情页的点赞 toggleLike、收藏 toggleCollect、评论 addComment 已通过 db.js 封装走 interact，但回调中拿到结果后需用返回的 counts 刷新 UI 而非本地 ±1。

在点赞回调中，检查返回值是否含 `likes`，有则 `setData({ 'post.likes': result.likes })`。
在收藏回调中，检查返回值是否含 `collectCount`，有则 `setData({ 'post.collectCount': result.collectCount })`。
在评论回调中，检查返回值是否含 `commentCount`，有则 `setData({ 'post.commentCount': result.commentCount })`。

- [ ] 改互动回调刷 UI

### Step 4: 页面验证

```bash
cd /f/molispark/mini && npm run validate:pages 2>&1 | tail -3
```
Expected: `Validated 15 registered routes and 15 page implementations.`

- [ ] 页面验证

### Step 5: Commit

```bash
cd /f/molispark/mini
git add pages/detail/index.js
git commit -m "feat(detail): 浏览/点赞/收藏/评论走 interact，按返回值刷 UI"
```

- [ ] Commit

---

## Task 7: v0.1.0 创建云数据库集合 + 配置权限 + 部署云函数（手动）

**说明：** 此 task 在微信开发者工具 / 云开发控制台手动完成，无代码改动。

### Step 1: 创建 7 个集合

微信开发者工具 → 云开发 → 数据库 → 新建集合：
`posts`、`comments`、`likes`、`collects`、`users`、`views`、`history`

- [ ] 创建 7 集合

### Step 2: 配置权限规则

- `posts`、`comments` → 所有用户可读，仅创建者可写
- `likes`、`collects`、`users`、`views`、`history` → 仅创建者可读写

- [ ] 配置权限

### Step 3: 部署 3 个云函数

右键每个云函数 →「上传并部署：云端安装依赖」：
`login`、`updateProfile`、`interact`

或用 CLI（需先开启服务端口，见项目 CLAUDE.md）：
```bash
& "F:\微信web开发者工具\cli.bat" cloud functions deploy --appid wxba805d188c9a4151 --env cloud1-d4gtpsssef2dcbcf8 --paths "F:\molispark\mini\cloudfunctions\login" "F:\molispark\mini\cloudfunctions\updateProfile" "F:\molispark\mini\cloudfunctions\interact" --remote-npm-install
```

- [ ] 部署 3 云函数

### Step 4: 验证部署

控制台确认：云函数列表含 login/updateProfile/interact；数据库集合列表含 7 个集合。

- [ ] 验证部署

---

## Task 8: v0.1.5 多人体验版端到端测试（手动）

**说明：** 需两个体验成员账号（A 作者、B 互动者），在微信开发者工具预览或体验版真机进行。

### Step 1: A 登录 + 完善资料 + 发帖

A 扫码进入 → 完善昵称头像 → 发布一篇帖子。
预期：`posts` 出现该记录（含 `collectCount: 0`，无 `comments` 字段），`users` 出现 A 的资料。

- [ ] A 发帖验证

### Step 2: B 浏览 A 的帖子

B 进入 → 打开 A 的帖子。
预期：`posts.views` +1（查云数据库确认），`views` 集合出现当天记录，`history` 集合出现快照。

- [ ] B 浏览验证

### Step 3: B 重复进入（防刷验证）

B 返回再进同一帖子。
预期：当天浏览量不再 +1（`views._id` 冲突），`history.viewedAt` 更新。

- [ ] B 防刷验证

### Step 4: B 点赞 + 取消点赞

B 点赞 → `likes` 新增 1 条（`_id = postId_OPENID`），`posts.likes` +1。
快速重复点赞 → 不产生重复文档。
取消点赞 → `likes` 删除，`posts.likes` -1（不低于 0）。

- [ ] B 点赞验证

### Step 5: B 收藏 + 取消收藏

B 收藏 → `collects` 新增，`posts.collectCount` +1。
取消收藏 → 删除，`posts.collectCount` -1。

- [ ] B 收藏验证

### Step 6: B 评论

B 发评论 → `comments` 新增，`posts.commentCount` +1，detail 评论列表刷新。

- [ ] B 评论验证

### Step 7: C 一致性核对

C 打开同一帖子 → 看到的计数与 A/B 操作后一致。

- [ ] C 一致性验证

### Step 8: 权限验证

控制台尝试用 B 的 openid 改 A 的帖子正文 → 被拒绝。
控制台尝试 B 读 `likes` 集合（含 C 的记录）→ 只能读自己的。

- [ ] 权限验证

### Step 9: 计数一致性核对

云数据库核对：`posts.likes` == `likes` 集合该 postId 记录数；`posts.commentCount` == `comments` 集合该 postId 记录数；`posts.collectCount` == `collects` 集合该 postId 记录数。

- [ ] 计数一致性验证

### Step 10: 无云降级验证

断网/关云环境 → 进入详情页 → 走 mock 兜底，但控制台有降级日志提示。

- [ ] 无云降级验证

---

## Task 9: v0.1.5 体验版发布 + 添加体验成员（手动）

### Step 1: 上传体验版

开发者工具 → 上传 → 版本号 `0.1.0`、备注「v0.1 核心闭环」。

- [ ] 上传体验版

### Step 2: 设为体验版

小程序后台 → 管理 → 版本管理 → 设为体验版。

- [ ] 设为体验版

### Step 3: 添加体验成员

管理 → 成员管理 → 体验成员 → 添加微信号（≤90 人）。

- [ ] 加体验成员

### Step 4: 分享二维码

体验成员扫码进入，完成 Task 8 同样的流程确认。

- [ ] 分享 + 验证

---

## Task 10: v0.1.6 文档与遗留清理

**Files:**
- Create/Update: `docs/cloudfunctions.md`

### Step 1: 写云函数文档

创建 `docs/cloudfunctions.md`，记录 3 个云函数：
- `login`：入参、返回值、用途
- `updateProfile`：入参（nickName, avatarUrl, brief）、OPENID 从 context 取、users._id = OPENID、upsert 逻辑
- `interact`：6 个 action（view/like/unlike/collect/uncollect/comment）、入参、统一返回结构、错误码（UNAUTHORIZED / MISSING_POST_ID / INVALID_ACTION / POST_NOT_FOUND / EMPTY_CONTENT / CONTENT_TOO_LONG / COUNT_UPDATE_FAILED / INTERNAL_ERROR）

- [ ] 写 cloudfunctions.md

### Step 2: 确认 database-schema.md 完整

确认 `docs/database-schema.md` 中 7 集合的 _id 策略、字段、权限都已记录，无遗漏。

- [ ] 确认 schema 完整

### Step 3: 标注 mock fallback 位置

在 db.js 中找到所有 `isCloudReady()` 降级分支，用注释标注 `// v0.2 待清理` 方便后续定位。

- [ ] 标注 mock fallback

### Step 4: Commit

```bash
cd /f/molispark/mini
git add docs/cloudfunctions.md utils/db.js
git commit -m "docs: 云函数接口文档 + 标注 v0.2 待清理的 mock fallback"
```

- [ ] Commit

---

## Self-Review

**1. Spec 覆盖（对照 spec v0.1.0–v0.1.6）：**
- v0.1.0 云环境基线 → Task 1, 7 ✓
- v0.1.1 登录资料闭环 → Task 2 ✓
- v0.1.2 发帖真实云 → Task 3 ✓
- v0.1.3 interact 云函数 → Task 4 ✓
- v0.1.4 前端接入 → Task 5, 6 ✓
- v0.1.5 多人验收 → Task 8, 9 ✓
- v0.1.6 文档清理 → Task 10 ✓

**2. 6 action 覆盖：**
- view → Task 4 实现，Task 6 调用 ✓
- like → Task 4 实现，Task 5 toggleLike 调用 ✓
- unlike → Task 4 实现，Task 5 toggleLike 调用 ✓
- collect → Task 4 实现，Task 5 toggleCollect 调用 ✓
- uncollect → Task 4 实现，Task 5 toggleCollect 调用 ✓
- comment → Task 4 实现，Task 5 addComment 调用 ✓

**3. 确定性 _id 幂等：**
- likes._id = `${postId}_${OPENID}` ✓
- collects._id = `${postId}_${OPENID}` ✓
- views._id = `${postId}_${OPENID}_${YYYYMMDD}` ✓
- history._id = `${postId}_${OPENID}` ✓

**4. 失败边界：**
- COUNT_UPDATE_FAILED 明确返回 ✓
- console.error 记录 postId/openid/action ✓

**5. 统一返回结构：**
- `{ success, action, postId, state, counts, data }` ✓
- 前端按 state/counts 刷 UI ✓

无问题。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-15-v0.1-cloud-backend-mvp.md`. 两种执行方式：

**1. Subagent-Driven（推荐）** — 每个 task 派发独立 subagent，task 间审查，迭代快
**2. Inline Execution** — 在本会话按 task 执行，批量推进 + checkpoint 审查
